# 🗺️ Sitemap Generator - Руководство

## Обзор

Продвинутый генератор sitemap на базе пакета **Spatie Laravel Sitemap** автоматически создает XML карты сайта для улучшения SEO и индексации поисковыми системами.

### ✨ Новые возможности с Spatie:

- **Профессиональная библиотека** - используется проверенный пакет Spatie
- **Автоматическое форматирование XML** - правильная структура и валидация
- **Расширенные возможности** - поддержка изображений, видео и новостей
- **Лучшая производительность** - оптимизированная генерация
- **Стандарты Google** - полная совместимость с требованиями поисковых систем

## 🚀 Возможности

### ✅ Что включено в sitemap:

- **Основные страницы**: Главная, О нас, Услуги, Портфолио, Контакты, Блог
- **Страницы возможностей**: Data Analytics, Cloud Services, Custom Development
- **Юридические страницы**: Privacy Policy, Terms of Service
- **Портфолио проекты**: Все опубликованные проекты
- **Статьи блога**: Все опубликованные статьи
- **Категории блога**: Все активные категории
- **Мультиязычность**: Поддержка английского и украинского языков
- **Hreflang**: Правильные ссылки для альтернативных языков

### 📁 Структура файлов:

```
public/
├── sitemap.xml                    # Главный индекс sitemap
├── sitemap-main.xml              # Основные страницы
├── sitemap-portfolio.xml         # Проекты портфолио
├── sitemap-blog.xml              # Статьи блога
├── sitemap-blog-categories.xml   # Категории блога
└── robots.txt                    # Файл для поисковых роботов
```

## 🛠️ Использование

### Команды Artisan

#### 1. Генерация sitemap
```bash
php artisan sitemap:generate
```

#### 2. Принудительная генерация (перезапись существующих файлов)
```bash
php artisan sitemap:generate --force
```

#### 3. Указание директории вывода
```bash
php artisan sitemap:generate --output=storage/app/public
```

#### 4. Обновление sitemap
```bash
php artisan sitemap:update
```

### 🆕 Новые возможности Spatie

#### Автоматическая генерация с помощью моделей
```php
use Spatie\Sitemap\Sitemap;
use App\Models\BlogPost;

// Автоматическая генерация из модели
Sitemap::create()
    ->add(BlogPost::all())
    ->writeToFile(public_path('sitemap.xml'));
```

#### Добавление изображений
```php
use Spatie\Sitemap\Tags\Url;

Url::create('/page')
    ->addImage('/image.jpg', 'Image description')
    ->addImage('/image2.jpg', 'Image description 2');
```

#### Настройка приоритетов и частоты
```php
Url::create('/page')
    ->setPriority(0.8)
    ->setChangeFrequency('weekly')
    ->setLastModificationDate(now());
```

### Автоматическое обновление

Sitemap автоматически обновляется ежедневно в 2:00 ночи через Laravel Scheduler.

Для работы автоматического обновления добавьте в crontab:
```bash
* * * * * cd /path/to/project && php artisan schedule:run >> /dev/null 2>&1
```

## 🌐 Доступ к sitemap

### URL-адреса:
- **Главный sitemap**: `https://yourdomain.com/sitemap.xml`
- **Основные страницы**: `https://yourdomain.com/sitemap-main.xml`
- **Портфолио**: `https://yourdomain.com/sitemap-portfolio.xml`
- **Блог**: `https://yourdomain.com/sitemap-blog.xml`
- **Категории блога**: `https://yourdomain.com/sitemap-blog-categories.xml`
- **Robots.txt**: `https://yourdomain.com/robots.txt`

## 📊 Приоритеты страниц

| Тип страницы | Приоритет | Частота обновления |
|--------------|-----------|-------------------|
| Главная | 1.0 | Weekly |
| Услуги | 0.9 | Weekly |
| О нас, Портфолио, Блог | 0.8 | Weekly/Monthly |
| Контакты | 0.7 | Monthly |
| Возможности | 0.7 | Monthly |
| Проекты портфолио | 0.6 | Monthly |
| Статьи блога | 0.6 | Monthly |
| Категории блога | 0.5 | Weekly |
| Юридические страницы | 0.3 | Yearly |

## 🔧 Настройка

### Добавление новых страниц

1. **Для статических страниц** - отредактируйте массив `$mainPages` в `SitemapController::main()`

2. **Для динамических страниц** - добавьте новый метод в `SitemapController` и соответствующий маршрут

3. **С использованием Spatie** - используйте готовые методы:
```php
// В контроллере
$sitemap = Sitemap::create();
$sitemap->add(Url::create('/new-page')
    ->setPriority(0.8)
    ->setChangeFrequency('monthly'));
```

### Изменение языков

Отредактируйте массив `$locales` в методах контроллера:
```php
$locales = ['en', 'uk', 'de']; // Добавить немецкий
```

### Настройка приоритетов

Измените значения `priority` и `changefreq` в соответствующих массивах страниц или используйте методы Spatie:
```php
Url::create('/page')
    ->setPriority(0.8)           // Приоритет от 0.0 до 1.0
    ->setChangeFrequency('weekly') // daily, weekly, monthly, yearly
    ->setLastModificationDate(now());
```

### Расширенные возможности Spatie

#### Добавление изображений к страницам
```php
Url::create('/portfolio/project-1')
    ->addImage('/images/project-1.jpg', 'Project 1 Screenshot')
    ->addImage('/images/project-1-mobile.jpg', 'Mobile Version');
```

#### Добавление видео
```php
Url::create('/video-page')
    ->addVideo('/videos/demo.mp4', 'Demo Video', 'Demo description');
```

#### Создание sitemap из коллекции моделей
```php
use App\Models\Portfolio;

Sitemap::create()
    ->add(Portfolio::published()->get())
    ->writeToFile(public_path('sitemap-portfolio.xml'));
```

## 🚀 Интеграция с поисковыми системами

### Google Search Console

1. Перейдите в [Google Search Console](https://search.google.com/search-console)
2. Добавьте свой сайт
3. Перейдите в раздел "Sitemaps"
4. Добавьте URL: `https://yourdomain.com/sitemap.xml`

### Яндекс.Вебмастер

1. Перейдите в [Яндекс.Вебмастер](https://webmaster.yandex.ru)
2. Добавьте свой сайт
3. Перейдите в раздел "Индексирование" → "Файлы Sitemap"
4. Добавьте URL: `https://yourdomain.com/sitemap.xml`

### Bing Webmaster Tools

1. Перейдите в [Bing Webmaster Tools](https://www.bing.com/webmasters)
2. Добавьте свой сайт
3. Перейдите в раздел "Sitemaps"
4. Добавьте URL: `https://yourdomain.com/sitemap.xml`

## 🐛 Устранение неполадок

### Проблема: Sitemap не генерируется
**Решение**: Проверьте права доступа к директории `public/`

### Проблема: Неправильные URL в sitemap
**Решение**: Убедитесь, что `APP_URL` правильно настроен в `.env`

### Проблема: Автоматическое обновление не работает
**Решение**: Проверьте настройку crontab и Laravel Scheduler

### Проблема: Ошибки в XML
**Решение**: Проверьте корректность данных в моделях Portfolio и BlogPost

## 📝 Логи и мониторинг

Команда генерации sitemap выводит подробную информацию о процессе:
- ✅ Успешно сгенерированные файлы
- ⚠️ Предупреждения о существующих файлах
- ❌ Ошибки при генерации

## 🔄 Обновление контента

Sitemap автоматически включает:
- Только опубликованные проекты портфолио (`is_published = true`)
- Только опубликованные статьи блога (`status = 'published'`)
- Только активные категории блога (`is_active = true`)

При добавлении нового контента sitemap обновится при следующем запуске команды или автоматически в 2:00 ночи.

## 🆕 Преимущества Spatie Laravel Sitemap

### ✅ Что улучшилось:

1. **Профессиональная библиотека** - используется проверенный пакет от команды Spatie
2. **Автоматическая валидация XML** - правильная структура и форматирование
3. **Расширенные возможности** - поддержка изображений, видео, новостей
4. **Лучшая производительность** - оптимизированная генерация больших sitemap
5. **Стандарты Google** - полная совместимость с требованиями поисковых систем
6. **Упрощенный код** - меньше ручного форматирования XML
7. **Автоматическое кеширование** - улучшенная производительность
8. **Поддержка CDN** - возможность загрузки на внешние сервисы

### 🔄 Миграция с предыдущей версии:

Старый код:
```php
$sitemap = '<?xml version="1.0" encoding="UTF-8"?>';
$sitemap .= '<urlset xmlns="...">';
// ... ручное формирование XML
```

Новый код с Spatie:
```php
$sitemap = Sitemap::create()
    ->add(Url::create('/page')
        ->setPriority(0.8)
        ->setChangeFrequency('weekly'))
    ->writeToFile($path);
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи Laravel: `storage/logs/laravel.log`
2. Убедитесь в корректности настроек базы данных
3. Проверьте права доступа к файловой системе
4. Обратитесь к [документации Spatie Laravel Sitemap](https://github.com/spatie/laravel-sitemap)

### 🐛 Устранение неполадок Spatie

**Проблема**: Ошибка "Class 'Spatie\Sitemap\Sitemap' not found"
**Решение**: Выполните `composer dump-autoload`

**Проблема**: Неправильное форматирование XML
**Решение**: Spatie автоматически форматирует XML, проверьте данные моделей

**Проблема**: Медленная генерация больших sitemap
**Решение**: Используйте чанки для больших коллекций:
```php
Portfolio::published()->chunk(100, function ($portfolios) use ($sitemap) {
    $sitemap->add($portfolios);
});
```
